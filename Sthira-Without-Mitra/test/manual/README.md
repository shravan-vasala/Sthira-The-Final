# Manual diagnostics

Run from the project root:

```sh
dart run test/manual/isar_instances.dart
```

This prints the Isar instance names in that standalone process. It is a diagnostic utility, not an automated test and not evidence about the running app's database.
